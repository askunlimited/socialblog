import { createTheme } from "@mui/material";
import { blue, red, black } from "@material-ui/core/colors";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#111111",
        },
        secondary: {
            main: "#fdffef",
        }
    },
    
})